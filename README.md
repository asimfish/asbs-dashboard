# ASBS research dashboard

Public, sanitized proposal and evidence board. Runtime JSON is maintained on the `data` branch; browser flags stale snapshots. Raw research data, server commands and credentials are not included.

Status labels distinguish execution from scientific validation.

Verification: six campaign boundary tests; desktop/mobile browser checks; stale/error and text-safety checks; independent numerical and physics canaries.

Refs: T-661
