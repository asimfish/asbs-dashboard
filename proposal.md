# ASBS：把经过验证的纠错经验变成冻结 VLA 的自主恢复

状态：方案 v2；方法优势未验证。这里介绍候选方案，不把计划当成实验结论。

## 我们解决什么问题

机器人已有可用的动作策略，但面对新的物理情境会重复犯错。高层 agent 可以诊断、试探或纠正一次；如果每次遇到相似情境都重新调用昂贵的教师，系统没有积累可复用的恢复能力。

我们研究：**能否利用少量经过实际执行验证的纠错经验，在不修改 VLA 骨干的情况下，学会在熟悉的失败情境中自主选择并连续执行更好的动作？**

输出是一个小型条件采样器。训练时允许教师和后果估计；熟悉情境部署时，不要求每个动作块都在线调用教师或查询 Q。未知情境的教师升级是独立机制，不在此处承诺自动可靠。

## ASBS 的具体作用

冻结的随机 VLA 将当前可见历史 h 和基础噪声 z 转成动作块：a = G(h,z)，z 的参考分布为标准高斯 γ。ASBS 学习在同一噪声空间中条件采样，把质量放到预测后续回报更高的区域；VLA 本身不更新。

它不是把 agent 写出的动作直接蒸馏进 VLA，也不是对已有候选简单排序。需要验证它确实能在相同计算预算下改善采样分布；能否产生有用动作受 G 的可达能力和后果估计准确性限制。

## 闭环学习

1. 收集实际执行前缀与随后观测，保留失败和教师干预，不把语言判断当成功真值。
2. 固定当前学生策略，估计其后续执行的 Q；终止转移不 bootstrap，非终止转移从该学生采样下一步动作。这样避免把教师接管的成功误归因给学生自主恢复。
3. 在固定基础先验下，构造偏向高 Q 的条件目标：q†(z|h) ∝ γ(z) exp(Q(h,G(h,z))/λ)。这不需要计算学生采样器的显式密度。
4. 用真正的 Adjoint/Corrector Matching 训练条件 ASBS；执行动作前缀后重新观察。每轮先在独立验证集检验，再考虑升级学生。

参考先验固定并不保证未正则化任务成功率逐轮单调提升。有限数据、Q外推、数值误差和噪声空间能力不足，都可能让此方案失败。

## 为什么值得研究，为什么现在还不能认领贡献

研究价值来自“昂贵的外部纠错能否积累为低成本自主恢复”，不是“第一次把ASBS放进机器人”。AMDP等方法已有Q与Adjoint Matching联系；仅换术语或拼装组件不构成独立新意。

必要对比包括同反馈和同预算下的加权回归、noise RL、AMDP、候选重排；报告任务成功、恢复、原能力保持、教师调用、训练/推理成本和端到端延迟。ASBS需显示独立收益或可解释的适用边界。

## 本轮八小时究竟验证什么

- 数值门：相同初始函数下，改变时间表示是否修复已观测的端点误差。三seed与AM对照，不能只看loss。
- 反馈门：新物理布局中，冻结模型实际选中的动作是否比原候选更好。先决定动作再取未来标签；保留进展指标，防止以停滞换安全。
- 这两项沿用已有研究基础，是前置诊断。它们既不是高维VLA闭环实验，也不能支持“论文已够中稿”。随机VLA接口、持续恢复和强算法对比仍需后续验收。

主板区分：未开始、运行中、执行完成、科学通过、未通过和前置阻塞。负结果和范围限制同样公开。

Refs: T-660（方案）/ T-661（验证与主板）
